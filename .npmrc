# NPM configuration for optimized builds on memory-constrained environments
# Reduce memory usage during npm install
legacy-peer-deps=false
fetch-retries=5
fetch-retry-mintimeout=30000
fetch-retry-maxtimeout=180000
maxsockets=1
prefer-offline=true
